<h1>Usuarios</h1>

<div class="usersContainer">
    No anda...
    {{#each users}}
    <div class="usersData" userId="{{this._id}}">
        <h2>{{this.first_name}} {{this.last_name}}</h2>
        <p>E-Mail: {{this.email}}</p>
        <p>Rol: {{this.role}}</p>
    </div>
    {{/each}}
</div>

{{#each users}}
<div class="usersData" userId="{{this._id}}">
    <h2>{{this.first_name}} {{this.last_name}}</h2>
    <p>E-Mail: {{this.email}}</p>
    <p>Rol: {{this.role}}</p>
</div>
{{/each}}